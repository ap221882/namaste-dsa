for (let i = 2; i < 9; i = i + 2) {
  console.log('====================================');
  console.log('Namaste world', i);
  console.log('====================================');
}

// 2, 4, 6, 8 - value of i's

/** //> FOR LOOP
 * Before the loop starts, it initialises the variable. 
 * Before each iteration, the condition is checked with the current value
 * after conditional fails or passes, the code inside for loop is executed
 * after executing the code, the value change(increment/decrement) happens
 */

/** //> WHILE LOOP
 */

let i = 7;

while (i > 0) {
  console.log('====================================');
  console.log('while loop', i);
  console.log('====================================');
  i--;
}